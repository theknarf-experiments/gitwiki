# Welcome to Git Wiki

This is a wiki solution that uses flat files ina Git repo as its database!
It's super easy to setup, just create an empty folder and run the following command:

```
git init
npx gitwiki init
```

That's it! Your new git wiki is setup, run `npm start` to start the solution.
Or read up on configuration options below.

[Test](test)
